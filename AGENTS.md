# AGENTS.md

The law for this repository is the global vault rulebook at `~/.claude/AGENTS.md`
and its modules in `~/.claude/.agent-rules/*.md` (notably `multitenancy.md`,
`laravel-advanced.md`, `inertia-react-advanced.md`, `security-2026.md`). Read it
before any task and follow it exactly; this project adds no overrides.
